using UnityEngine;
using System.Collections;

/// <summary>
/// Serves as the base class for regions on the map such as road, water
/// </summary>
public abstract class SubRegion : MonoBehaviour {
    protected Vector2Int lowerBound;
    protected Vector2Int upperBound;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
